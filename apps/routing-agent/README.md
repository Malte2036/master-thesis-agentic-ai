# routing-agent

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build routing-agent` to build the library.

## Running unit tests

Run `nx test routing-agent` to execute the unit tests via [Jest](https://jestjs.io).
